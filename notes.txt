
Call graph

- supply argument for with jvm version to support multi-release jar
    - support strong classname with jvm version: jar-file@version#FQN
        -examples of "strong" class names
            -jar-file#org.kapa.cli.Main
            -jar-file@17#org.kapa.cli.Main

- linkage step
    - fill foreign columns
    - update refCount
    - create reports for:
        -symbol not found
        -duplicate symbol
        -private symbol with zero refCount

- query graph
    - use similar query language I developed to query the digest table
    - specialized query statement for graph-like structures
        - list all affected classes if a given node is modified - we super granularity to improve tests
