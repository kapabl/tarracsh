package(default_visibility = ["//visibility:public"])

cc_library(
    name = "types_lib",
    hdrs = glob(["*.h"], allow_empty = True),
    copts = ["-std=c++20"],
    includes = ["../.."],
    deps = [
        "//src/domain/classfile:classfile_constants",
    ],
)
