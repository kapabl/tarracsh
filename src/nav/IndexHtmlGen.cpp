#include "IndexHtmlGen.h"


using namespace org::kapa::tarracsh::nav;

void IndexHtmlGen::generate() {
    //TODO walk all the dirs affected my the cpool html gen
    //and check how many files are there and generate and index
    //that will redirect if there is only 1 impl or generate a list
    //if there is more than 1 impl
}
