package(default_visibility = ["//visibility:public"])

cc_library(
    name = "proto_lib",
    srcs = [
        "Server.pb.cc",
        "Server.grpc.pb.cc",
    ],
    hdrs = [
        "Server.pb.h",
        "Server.grpc.pb.h",
        "Server.proto",
    ],
    copts = ["-std=c++20"],
    includes = ["../../../../"],
    deps = [
        "//:vcpkg_headers",
    ],
)
