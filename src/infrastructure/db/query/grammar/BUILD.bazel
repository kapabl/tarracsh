package(default_visibility = ["//visibility:public"])

cc_library(
    name = "grammar_lib",
    srcs = glob(["*.cpp"], allow_empty = True),
    hdrs = glob(["*.h"], allow_empty = True),
    copts = ["-std=c++20"],
    defines = ["FMT_HEADER_ONLY=1"],
    includes = ["../../../../"],
    deps = [
        "//:vcpkg_headers",
        "//src/infrastructure/db:db_core",
        "//src/infrastructure/db/table:table_lib",
    ],
)
