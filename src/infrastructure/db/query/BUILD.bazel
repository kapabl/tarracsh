package(default_visibility = ["//visibility:public"])

cc_library(
    name = "query_lib",
    srcs = ["Engine.cpp"],
    hdrs = ["Engine.h"],
    copts = ["-std=c++20"],
    defines = ["FMT_HEADER_ONLY=1"],
    includes = ["../../.."],
    deps = [
        "//:vcpkg_headers",
        "//src/infrastructure/db:db_core",
        "//src/infrastructure/db/query/grammar:grammar_lib",
        "//src/infrastructure/string:string_lib",
    ],
)
