package(default_visibility = ["//visibility:public"])

cc_library(
    name = "table_lib",
    srcs = [
        "Table.cpp",
        "DatabaseBridge.cpp",
        "ColumnRefFormatter.cpp",
    ],
    hdrs = ["Table.h"],
    copts = ["-std=c++20"],
    defines = ["FMT_HEADER_ONLY=1"],
    includes = ["../../.."],
    deps = [
        "//:vcpkg_headers",
        "//src/infrastructure/db:db_core",
        "//src/infrastructure/db/columns:columns_lib",
    ],
)
