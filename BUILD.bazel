load("@rules_cc//cc:defs.bzl", "cc_binary")


cc_binary(
    name = "tarrash",
    srcs = glob(["src/**/*.cpp"]),
    includes = ["src", "src/classfile"],
    copts = ["/std:c++20"],
    deps = [

    ]
#    hdrs = ["*.h"]
)